# Optional: connect to a real Supabase project.
# Leave these unset (or blank) to run the app in local demo mode, which
# stores all data in the browser's localStorage instead.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
